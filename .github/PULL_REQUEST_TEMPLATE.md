## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `pytest` passes
- [ ] Tests added or updated for the change
- [ ] Docs updated (`README.md` / `docs/`) if behaviour or the public API changed
- [ ] For a new or changed loader: annotated with `@modality_meta`, and
      `./gen_loaders.sh` re-run with the result committed
